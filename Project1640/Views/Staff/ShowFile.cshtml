@model IEnumerable<Project1640.Models.FileUpload>

@foreach (var item in Model)
{
        <tr>
            <span>File Upload: </span>  <a href="#">@Html.DisplayFor(modelItem => item.Name)</a>
        </tr>
}

